"""Vendored shape-vs-texture bias analysis (Geirhos cue-conflict).

Copied from the standalone ``shape-bias-analysis`` repo so the analysis ships with
ares and runs on the aircc/slurm clusters (which do not have that repo). See the
per-module provenance headers. Only the three self-contained modules needed for
scoring are vendored: ``mapping``, ``metrics``, ``dataset``.
"""
